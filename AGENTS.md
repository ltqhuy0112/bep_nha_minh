@C:\Users\adam.le\.codex\RTK.md

## Project Skill

For work in this repository, use the project-local skill:

`skills/bep-nha-minh-phase-runner/SKILL.md`

Use it when continuing phases, editing the app, debugging Docker/PostgreSQL/API behavior, reviewing code, or planning/installing additional MCP/plugin capabilities for this project.

## CodeGraph

This repository may contain `.codegraph/`. If it exists, use CodeGraph before grep/find or broad file reading when understanding code.

Preferred MCP route: use `tool_search` to load `codegraph_explore` if available.

Shell fallback:

```powershell
rtk codegraph explore "<symbol names or question>"
```

## Context7

Use Context7 for current library/API documentation before changing Next.js, React, Tailwind, Docker Compose, PostgreSQL, `pg`, `node-pg-migrate`, Zod, or Motion usage.
